{
  "name": "arina_kuryashkina_portfolio",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    // ... остальные клиентские зависимости, но УДАЛИТЬ express, etc.
  },
  "devDependencies": {
    // ... остальные клиентские devDependencies, но УДАЛИТЬ @types/express, esbuild, tsx, etc.
  }
}
